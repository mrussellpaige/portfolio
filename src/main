import React from "react";
import ReactDOM from "react-dom/client";
import PortfolioWebsiteStarter from "./index"; // or App if renamed

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PortfolioWebsiteStarter />
  </React.StrictMode>
);
